# yolo-flutter-app (官方示例) 项目配置指南

> 基于 Ultralytics 官方 yolo-flutter-app 仓库的示例应用，使用本地 YOLO 模型实现离线目标检测。

## 目录

- [前置环境要求](#前置环境要求)
- [操作步骤](#操作步骤)
  - [1. 获取源码](#1-获取源码)
  - [2. 准备本地 TFLite 模型](#2-准备本地-tflite-模型)
  - [3. 配置国内 Gradle/Maven 镜像（可选）](#3-配置国内-gradlemaven-镜像可选)
  - [4. 修改代码使用本地模型](#4-修改代码使用本地模型)
  - [5. 安装依赖](#5-安装依赖)
  - [6. 构建并运行](#6-构建并运行)
- [关键文件清单](#关键文件清单)
- [项目结构](#项目结构)
- [与 yolo_app 项目的区别](#与-yolo_app-项目的区别)
- [常见问题](#常见问题)

---

## 前置环境要求

| 工具 | 版本要求 | 说明 |
|------|---------|------|
| Flutter SDK | >= 3.32.1 | `flutter --version` 检查 |
| Dart SDK | >= 3.8.1 | 随 Flutter 一起安装 |
| Android Studio | 最新版 | 提供 Android SDK 和模拟器 |
| Python + ultralytics | 8.x | 用于导出 TFLite 模型 |
| 安卓手机 | Android 14+ (API 34) | 需开启 USB 调试 |

> **注意：** 此项目对 Flutter/Dart SDK 版本要求高于 yolo_app 项目（需要 Flutter >= 3.32.1）。

---

## 操作步骤

### 1. 获取源码

从 GitHub 下载或克隆官方仓库：

```bash
git clone https://github.com/ultralytics/yolo-flutter-app.git
cd yolo-flutter-app
```

或手动下载 ZIP 后解压。项目结构如下：

```
yolo-flutter-app-main/
├── lib/                  # 插件源码（ultralytics_yolo）
├── example/              # ← 示例应用（我们操作的目标）
│   ├── lib/
│   ├── android/
│   ├── ios/
│   └── pubspec.yaml
└── ...
```

> **重要：** 示例应用在 `example/` 子目录中，所有后续操作都在 `example/` 目录下进行。

### 2. 准备本地 TFLite 模型

使用 Python ultralytics 库导出 TFLite 格式模型：

```bash
pip install ultralytics
python -c "from ultralytics import YOLO; m = YOLO('yolo11n.pt'); m.export(format='tflite', imgsz=640)"
```

将导出的模型复制到 example 项目的 assets 目录：

```bash
# 创建目录
mkdir -p example/assets/models

# 复制模型
cp yolo11n_saved_model/yolo11n_float32.tflite example/assets/models/yolo11n.tflite
```

> **说明：** 官方项目原本有 `scripts/fetch_bundled_models.sh` 脚本自动下载模型到 `assets/models/`，但需要 bash 环境和稳定网络。手动放入模型文件更可靠。

### 3. 配置国内 Gradle/Maven 镜像（可选）

如果在国内网络环境下构建超时，需要配置镜像加速。

**`example/android/settings.gradle`** — 在 `pluginManagement.repositories` 中添加：

```groovy
repositories {
    maven { url 'https://maven.aliyun.com/repository/google' }
    maven { url 'https://maven.aliyun.com/repository/central' }
    maven { url 'https://maven.aliyun.com/repository/gradle-plugin' }
    google()
    mavenCentral()
    gradlePluginPortal()
}
```

**`example/android/build.gradle`** — 在 `allprojects.repositories` 中添加：

```groovy
allprojects {
    repositories {
        maven { url 'https://maven.aliyun.com/repository/google' }
        maven { url 'https://maven.aliyun.com/repository/central' }
        google()
        mavenCentral()
    }
}
```

**`example/android/gradle/wrapper/gradle-wrapper.properties`** — 替换 Gradle 下载源：

```properties
distributionUrl=https\://mirrors.cloud.tencent.com/gradle/gradle-8.14-all.zip
```

### 4. 修改代码使用本地模型

编辑 `example/lib/presentation/screens/camera_inference_screen.dart`。

**原始代码**使用 `YOLOShowcase` 组件（自动从网络下载模型）：

```dart
// 原始代码
@override
Widget build(BuildContext context) {
  return Scaffold(
    body: YOLOShowcase(versionLabel: _versionLabel, onCapture: _onCapture),
  );
}
```

**修改后**使用 `YOLOView` + 本地模型路径：

```dart
// 修改后的代码
static const _localModel = 'assets/models/yolo11n.tflite';

@override
Widget build(BuildContext context) {
  return Scaffold(
    body: Stack(
      children: [
        Positioned.fill(
          child: YOLOView(
            modelPath: _localModel,
            task: YOLOTask.detect,
            controller: _controller,
            onResult: _onResult,
            onPerformanceMetrics: _onPerformanceMetrics,
          ),
        ),
        // ... UI 覆盖层（暂停遮罩、信息栏、控制按钮等）
      ],
    ),
  );
}
```

> **核心变更：**
> - 移除 `YOLOShowcase` 依赖，改用底层 `YOLOView` 组件
> - `modelPath` 指向本地 assets 路径而非官方模型 ID
> - 自行管理推理结果回调、暂停/继续、摄像头切换等逻辑

完整修改后的代码见 `example/lib/presentation/screens/camera_inference_screen.dart`。

### 5. 安装依赖

```bash
cd example
flutter pub get
```

> **注意：** 此项目的 `ultralytics_yolo` 依赖是通过 `path: ../` 引用本地插件源码，而非 pub.dev 上的发布版本。这意味着你可以直接修改插件源码来定制功能。

### 6. 构建并运行

```bash
# 分析代码
flutter analyze

# 连接手机后检查设备
flutter devices

# Debug 模式运行
flutter run -d <设备ID>

# Release 模式运行
flutter run -d <设备ID> --release
```

---

## 关键文件清单

| 文件路径 | 用途 |
|---------|------|
| `example/pubspec.yaml` | 依赖声明（ultralytics_yolo 通过 path 引用本地源码） |
| `example/lib/main.dart` | 应用入口，路由配置 |
| `example/lib/presentation/screens/camera_inference_screen.dart` | **核心修改文件** — 本地模型实时检测 |
| `example/assets/models/yolo11n.tflite` | 本地 YOLO TFLite 模型（~10.3MB） |
| `example/android/app/src/main/AndroidManifest.xml` | Android 摄像头和网络权限 |
| `example/android/app/build.gradle` | Android 构建配置（含模型自动下载脚本） |
| `example/android/settings.gradle` | Gradle 插件管理 + Maven 镜像 |
| `example/android/build.gradle` | 根 Gradle + Maven 镜像 |
| `example/android/gradle/wrapper/gradle-wrapper.properties` | Gradle 下载源 |

## 项目结构

```
yolo-flutter-app-main/
├── lib/                                # ultralytics_yolo 插件源码
│   ├── ultralytics_yolo.dart
│   ├── yolo.dart
│   ├── yolo_view.dart                  # YOLOView 核心组件
│   ├── widgets/
│   │   ├── yolo_controller.dart        # YOLOViewController
│   │   └── yolo_showcase.dart          # 高级 Showcase UI（在线模型）
│   └── core/
│       ├── yolo_model_manager.dart     # 模型下载管理
│       └── yolo_model_resolver.dart    # 模型路径解析
│
├── example/                            # ← 示例应用
│   ├── assets/
│   │   └── models/
│   │       └── yolo11n.tflite          # 本地 TFLite 模型
│   ├── lib/
│   │   ├── main.dart                   # 入口
│   │   └── presentation/
│   │       └── screens/
│   │           ├── camera_inference_screen.dart  # 摄像头检测（已改为本地模型）
│   │           └── single_image_screen.dart      # 单张图片检测
│   ├── android/
│   │   ├── app/
│   │   │   ├── build.gradle            # 构建配置
│   │   │   └── src/main/
│   │   │       └── AndroidManifest.xml # 权限
│   │   ├── build.gradle
│   │   ├── settings.gradle
│   │   └── gradle/wrapper/
│   │       └── gradle-wrapper.properties
│   └── pubspec.yaml                    # 依赖（path: ../ 引用插件）
│
├── scripts/
│   └── fetch_bundled_models.sh         # 官方模型自动下载脚本
└── pubspec.yaml                        # 插件 pubspec
```

## 与 yolo_app 项目的区别

| 对比项 | yolo_app | yolo-flutter-app (example) |
|--------|---------|--------------------------|
| 项目类型 | 自建 Flutter 项目 | 官方仓库示例 |
| ultralytics_yolo 来源 | pub.dev（`^0.6.5`） | 本地源码（`path: ../`） |
| Gradle 版本 | 9.1.0 | 8.14 |
| Gradle 配置格式 | Kotlin DSL (`.gradle.kts`) | Groovy (`.gradle`) |
| Android 包名 | `com.example.yolo_app` | `com.ultralytics.yolo` |
| UI 复杂度 | 简洁自定义 UI | 原版使用 YOLOShowcase（含任务/模型选择），修改后为简洁自定义 UI |
| 功能 | 纯摄像头实时检测 | 原版支持 6 种任务（Detect/Segment/Semantic/Classify/Pose/OBB）+ 单张图片检测 |
| 可定制性 | 高（完全自主） | 中（可修改插件源码） |
| Flutter 版本要求 | >= 3.12.2 | >= 3.32.1 |

## 常见问题

### Q: 原始项目使用 `YOLOShowcase` 自动下载模型，为什么要改成 `YOLOView`？

`YOLOShowcase` 内部硬编码了 `yolo26` 系列模型 ID，会从 GitHub Releases 自动下载。在国内网络环境下下载经常失败。改用 `YOLOView` + 本地模型路径可以完全避免网络依赖。

### Q: 如何恢复官方的多任务切换功能？

如果需要 Detect / Segment / Pose 等多种任务切换，需要使用 `YOLOShowcase` 组件并确保每种任务的模型都已下载到本地。但这需要为每个任务单独导出对应的 TFLite 模型。

### Q: 如何使用自己训练的模型？

将自训练的 `.pt` 模型用 ultralytics 导出为 TFLite 格式，替换 `assets/models/yolo11n.tflite` 即可：

```bash
python -c "from ultralytics import YOLO; m = YOLO('your_model.pt'); m.export(format='tflite', imgsz=640)"
```

### Q: `fetchBundledModels` 脚本报错？

官方 `example/android/app/build.gradle` 中有个 `fetchBundledModels` 任务会尝试用 bash 运行脚本下载模型。在 Windows 上没有 bash 环境会报 warning（但不会中断构建，因为设置了 `ignoreExitValue = true`）。如果不需要自动下载，可以忽略此警告。

### Q: 如何打包 Release APK？

```bash
cd example
flutter build apk --release
```

生成的 APK 位于 `example/build/app/outputs/flutter-apk/app-release.apk`。
